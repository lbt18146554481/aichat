DATABASE_URL=postgresql://maitri:maitri@127.0.0.1:5433/maitri
SESSION_SECRET=change-me-to-a-long-random-string
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
NODE_ENV=development
WS_PORT=3001
# Browser WebSocket URL (optional; default = same host :3001)
# VITE_WS_URL=ws://127.0.0.1:3001
#
# Production (Nginx /ws reverse proxy on public IP):
# NODE_ENV=production
# VITE_WS_URL=ws://13.251.22.192/ws
# HTTP (no TLS) must keep cookies non-Secure:
# COOKIE_SECURE=0
# After you terminate HTTPS, set:
# COOKIE_SECURE=1

