services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: maitri
      POSTGRES_PASSWORD: maitri
      POSTGRES_DB: maitri
    ports:
      - "5433:5432"
    volumes:
      - maitri_pg:/var/lib/postgresql/data
volumes:
  maitri_pg:
